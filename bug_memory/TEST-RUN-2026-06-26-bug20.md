<h1 align="center">🧪 Bug #20 — 3-Level Test-User Run</h1>

<p align="center">
  <b>bin/floor on-hand double-count fix — full verification.</b><br>
  Date: <b>2026-06-26</b> · Run by: <b>test user</b> (<code>regression@test.com</code>) · Container <code>stockandpick_webapp</code> (kosh-web_app rebuilt <code>--no-cache</code>) · commit <code>cd542da</code>
</p>

---

## Headline

| Level | Scope | Result |
|---|---|---|
| **1 — Code testing** | regression suite (in-container) | **28 / 28 PASS** |
| **1b — Per-bug verifiers** | bugs 1–20 static checks | **20 / 20 PASS** |
| **2 — Affected views as test user** | Warehouse Inventory vs PCN History | **12 / 12 PASS** — all 10 PCNs match |
| **3 — Full workflow as test user** | login → … → restock | **8 / 8 PASS** |
| Live guard effect | `reconcile_floor_onhand` | 19 floor rows de-duped, 1,637 phantom units removed (audit-logged) |

---

## Level 1 — Code testing (`tests/run.sh`)

`28 passed, 0 failed.` New guards added for bug #20:
- `test_floor_onhand_dedupe_zeroes_phantom_bin_not_floor_or_bins` — floor guard zeroes phantom bin on-hand on MFG-floor rows, leaves mfg_qty intact, and does NOT touch clean floor rows or bin-located rows. **PASS**
- `test_pcn_history_anchor_counts_mfg_floor_stock` — PCN History anchor now = onhand + mfg (a floor-resident part shows N, not 0). **PASS**

All 20 per-bug verifiers (`bug_memory/*/verify-*.py`) pass.

## Level 2 — Affected views, logged in as the test user

GET `/pcn-history` → 200; GET `/warehouse-inventory` → 200 (as `regression@test.com`).

| PCN | Warehouse total | PCN History | Matches? | De-doubled? |
|---|---|---|---|---|
| 29862 | 140 | 140 | ✅ | yes (floor) |
| 40003 | 90 | 90 | ✅ | yes (floor) |
| 40005 | 90 | 90 | ✅ | yes (floor) |
| 40046 | 40 | 40 | ✅ | yes (floor) |
| 40007 | 20 | 20 | ✅ | yes (floor) |
| 37921 | 120 | 120 | ✅ | PENDING bin decision |
| 40044 | 20 | 20 | ✅ | PENDING bin decision |
| 42625 | 16 | 16 | ✅ | PENDING bin decision |
| 39355 | 6 | 6 | ✅ | PENDING bin decision |
| 44833 | 6 | 6 | ✅ | PENDING bin decision |

**Warehouse Inventory now matches PCN History for every affected PCN** (both use the bin+floor total). The 5 floor rows are also de-doubled to the true single qty. The 5 bin-located rows still show their internal double (e.g. 37921 = 60+60 = 120) and are **left untouched pending an explicit bin-vs-floor decision** — they are NOT auto-mutated.

## Level 3 — Full website walkthrough as the test user

**3a. Page-render smoke** (`test-level3-full-workflow-as-user.py`) — all render for `regression@test.com`, no 5xx/404: `GET /`, `/warehouse-inventory`, `/warehouse-inventory?search_pcn=29862`, `/shortage_report`, `/pcn-history`, `/pick`, `/restock` → all 200; session identity = test user. **8/8.**

**3b. Full job DATA flow** (`test-level3-full-job-flow-as-user.py`) — the real first-to-last workflow on synthetic PCN 99950 / job E2E-JOB-9950 (cleaned up after), driven as the test user. Checks Warehouse Inventory vs PCN History at every stage:

| Step | What happened | Result |
|---|---|---|
| 1. Upload job BOM | `/api/bom/load` inserted 1 line | ✅ |
| 2. Shortage report | 100 on hand covers 30 required → 0 false shortages | ✅ |
| 3. Check both (start) | Warehouse == PCN History == **100**; bin 100 / floor 0 | ✅ |
| 4. Pick 30 | `/api/pick` | ✅ |
| 5. Check both (after pick) | Warehouse == PCN History == **100**; bin **70** / floor **30** (moved to floor, total conserved) | ✅ |
| 6. Restock 30 | `restock_pcb` | ✅ |
| 7. Check both (after restock) | Warehouse == PCN History == **100**; bin **100** / floor **0** — **NO double (100, not 200)** | ✅ |

Step 7 is the exact bug #20 scenario: pre-fix the restock would have left the row doubled (e.g. 200); it now stays at the true 100, and both screens agree at every step. **ALL PASS.**

---

## Bin-located rows — verified and resolved

The 5 **bin-located** doubles were each traced (`remediation-bin-rows-applied.sql`):
every one is received → **picked to MFG Floor** → recounted → **put away/restocked back into a bin** (37921→1603002, 40044→1606102, 42625→2103504, 39355→2103504, 44833→1603002). So the parts are physically **in the bin** (`onhandqty`) and the `mfg_qty` is a **stale floor count**. Zeroed the stale `mfg_qty` only (on-hand untouched → no stock lost), audit-logged as `bug20_bin_stale_mfg_zeroed`.

**Final state: whole-table `remaining_doubles = 0`.** Re-run of Level 2 shows all 10 PCNs both **match** (Warehouse = PCN History) and are **de-doubled** to the true single qty:

| PCN | on-hand | location |
|---|---|---|
| 29862 | 140 (floor) | MFG Floor |
| 40003 | 90 (floor) | MFG Floor |
| 40005 | 90 (floor) | MFG Floor |
| 40046 | 40 (floor) | MFG Floor |
| 40007 | 20 (floor) | MFG Floor |
| 37921 | 60 (bin) | 1603002 |
| 40044 | 10 (bin) | 1606102 |
| 42625 | 8 (bin) | 2103504 |
| 39355 | 3 (bin) | 2103504 |
| 44833 | 3 (bin) | 1603002 |

Note: this is a one-time verified correction, not a blanket guard — a legitimate partial-pick split also has onhand+mfg both >0, so auto-zeroing mfg on bin rows would lose floor stock. New occurrences are surfaced by the integrity monitor for verified correction.
